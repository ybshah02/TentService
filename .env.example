APP_NAME=TentService
DEBUG=false
# Set to false in production to disable /api/v1/debug/* routes
DEBUG_ROUTES_ENABLED=true

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=  # anon key - required for JWT verification (works with asymmetric signing keys)
SUPABASE_SECRET_KEY=
SUPABASE_JWT_SECRET=  # legacy only - use if not using Auth server verification
DATABASE_URL=